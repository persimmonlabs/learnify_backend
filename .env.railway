# Railway Environment Variables Template
# Copy these to Railway dashboard: Settings > Variables

# === SERVER CONFIGURATION ===
SERVER_PORT=8080
SERVER_HOST=0.0.0.0
SERVER_ENV=production

# === DATABASE CONFIGURATION ===
# Railway will automatically provide DATABASE_URL
# You can also use individual variables:
# DATABASE_HOST=<provided-by-railway>
# DATABASE_PORT=5432
# DATABASE_USER=<provided-by-railway>
# DATABASE_PASSWORD=<provided-by-railway>
# DATABASE_NAME=railway
DATABASE_SSLMODE=require

# === JWT CONFIGURATION ===
# CRITICAL: Generate a secure random secret (min 32 characters)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-super-secure-jwt-secret-min-32-chars-CHANGE-THIS
JWT_EXPIRATION=86400

# === AI CONFIGURATION ===
# Get your OpenAI API key from: https://platform.openai.com/api-keys
AI_PROVIDER=openai
AI_API_KEY=sk-your-openai-api-key-here
AI_MODEL=gpt-4

# Optional: Use Anthropic Claude instead
# AI_PROVIDER=anthropic
# AI_API_KEY=sk-ant-your-anthropic-key-here
# AI_MODEL=claude-3-opus-20240229

# === CORS CONFIGURATION ===
# Add your Railway frontend URL after deployment
# Format: https://your-frontend.up.railway.app
CORS_ALLOWED_ORIGINS=https://learnify.up.railway.app,http://localhost:5173

# === LOGGING ===
LOG_LEVEL=info

# === NOTES ===
# 1. Set JWT_SECRET to a secure random string (use: openssl rand -base64 32)
# 2. Add your OpenAI or Anthropic API key
# 3. Update CORS_ALLOWED_ORIGINS with your Railway frontend URL after deployment
# 4. Railway automatically provides DATABASE_URL - no need to set manually
# 5. Keep DATABASE_SSLMODE=require for Railway PostgreSQL
